class Table

Attributes:
cleaned = (boolean)
can_seat_this_many = (interger)
seated = (interger)
names_of_customers_at_table = (array)
is_communal = (boolean)

Methods:
add_customer = (would add name to array and add one to seated)

table_capacity_left = (checks to see if it is a is communal table then subtracts
seated from can_seat_this_many)

will_seat = (checks if cleaned = true and then returns the number of available seats at table )

clear_table = (deletes names from array, sets seated to 0,  changes cleaned to false)
